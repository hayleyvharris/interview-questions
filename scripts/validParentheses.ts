// Given a string containing only {,},[,],(,) characters, determine if the sequence is valid.
// A valid sequence means that for every open brace, bracket and parentheses, there is a corresponding closing one.
// Note that these open and closing pairs can be nested.
// ({}[])(){([()])} --> valid
// ({{)[](){{]} --> invalid

function isValid(sequence: string): boolean {
    return false;
}

export default isValid;